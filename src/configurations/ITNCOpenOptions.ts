/*
import SerialPort from 'serialport'

export interface ITNCOpenOptions extends SerialPort.OpenOptions {
    // KissTNC specific values
    // TODO: Shouldn't these be in TNCSettings?
    callsign?: string
    unproto?: string
    fullDuplex?: boolean
    persistence?: number
    slotTime?: number
    txDelay?: number
    txTail?: number
}
*/